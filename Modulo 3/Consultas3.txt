select * from marca;

INSERT INTO marca values (null, 'Pepsi', 'USA');
INSERT INTO marca (nombre, pais_origen) values ('Ginger ale', 'Canada');
INSERT INTO marca (id_marca, nombre, pais_origen) values (3, 'Free', 'Chile');
INSERT INTO marca (nombre, pais_origen) values ('Sorbete Letelier', 'Chile');

Update marca set
nombre = 'Ginger Ale'
Where id_marca = 2;

-- Ejercicio 14: Modificar a mayuscula todas las marcas. Ojo con el permiso SAFE Mode SET SQL_SAFE_UPDATES = 0;
Update marca set nombre = upper(nombre)

update marca
set nombre = lower(nombre)
-- where nombre in ('PEPSI', 'FREE');
where id_marca in (1,3);

-- Ejercicio 16: agregar 3 categorias de producto para un supermercado 

select * from categoria;

insert into categoria (nombre, descripcion) values ('Bebestibles', 'todo tipo de jugos y bebidas');
insert into categoria (nombre, descripcion) values 
('Licor', 'Todo tipo de bebidas alcoholicas'),
('Perfumeria', 'Aseo personal, shampoo y similares, otros articulos de cuidado');


select * from categoria;
DELETE FROM categoria WHERE id_categoria = 2;
DELETE FROM categoria; -- siempre evitar usar de esta manera la sentencia. Borra toda la info

ALTER TABLE categoria AUTO_INCREMENT = 1;

truncate table producto; -- evitar por estar con fk
truncate table categoria;


set autocommit = 0;
select * from categoria;
DELETE FROM categoria; 
commit;
rollback;

